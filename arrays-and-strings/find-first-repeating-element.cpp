/* either use sorting O(nlog(n))
or use comparisons with each one O(n2)
or use hashing O(n)
*/

#include <iostream>
using namespace std;

int findFirstRepeatingElement(int* arr, int size) {

}

int main() {
	int arr[10] = { 1, 2, 3, 4, 5, 7, 2, 8, 9, 10 };

	cout<<findFirstRepeatingElement(arr, 10);
}